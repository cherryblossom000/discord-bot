import type {RegexCommand} from '../types'

const command: RegexCommand = {
  regex:
    /capitalism|capitalist|scum|elon|musk/ui,
  regexMessage: 'Kill all the capitalist scum!'
}
export default command
