{
  "name": "@comrade-pingu/commitlint-plugin",
  "version": "1.0.1",
  "private": true,
  "publishConfig": {
    "access": "restricted"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cherryblossom000/comrade-pingu",
    "directory": "packages/commitlint-plugin"
  },
  "main": "dist",
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "lint": "eslint --max-warnings 0 --ext ts --cache .",
    "build": "tsc -b",
    "watch": "dum build -- -w"
  },
  "devDependencies": {
    "@commitlint/types": "^16.0.0"
  }
}
