{
  "extends": "../../../tsconfig.settings.json",
  "include": ["**/*.ts", "../package.json"],
  "compilerOptions": {
    // Needs to be in src for deployment
    "rootDir": "..",
    "outDir": "../dist",
    "types": ["node"],
    "resolveJsonModule": true
  }
}
