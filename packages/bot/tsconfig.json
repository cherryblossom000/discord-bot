{
  // This file is required for ESLint to work both on the command line and in
  // VSCode. VSCode executes ESLint in the root directory, whereas the lint
  // script runs it in packages/semantic-release.
  "extends": "../../tsconfig.json",
  "include": ["**/*.ts"]
}
